import { ResetPasswordAuth } from '@/components/Auth'
import { AuthContainer } from '@/components/common'

export default function ResetPassword() {
   return (
      <AuthContainer>
         <ResetPasswordAuth />
      </AuthContainer>
   )
}
